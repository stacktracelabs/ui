interface:
  display_name: "Update shadcn-vue Components"
  short_description: "Upgrade components while preserving local changes"
  default_prompt: "Use $update-shadcn-vue-components to check for a newer stable shadcn-vue release, backport release-note-driven changes into components/, preserve CUSTOMIZATIONS, and validate the registry and docs consumer."
