<!-- Generated by scripts/generate-boost-skill-docs.mjs. Do not edit directly. -->

# Popover

Displays rich content in a positioned portal, opened by a control.

## Contents

- [Installation](#installation)
- [Usage](#usage)
- [State and focus](#state-and-focus)
- [Positioning and portals](#positioning-and-portals)
- [Composition](#composition)

## Installation

Add the Popover component from the configured StackTrace registry.

```shell
npx shadcn-vue@latest add @stacktrace/popover
```

## Usage

Use Popover for a small, contextual interaction such as editing a few settings, choosing a value, or showing details on demand. The trigger should make the purpose clear before the popover opens, and the content should stay short enough to understand without becoming a separate screen.

```vue
<template>
  <Popover>
    <PopoverTrigger as-child>
      <Button variant="outline">
        Open popover
      </Button>
    </PopoverTrigger>
    <PopoverContent class="w-80">
      <div class="grid gap-4">
        <div class="space-y-2">
          <h4 class="font-medium leading-none">
            Dimensions
          </h4>
          <p class="text-sm text-muted-foreground">
            Set the dimensions for the layer.
          </p>
        </div>
        <div class="grid gap-2">
          <div class="grid grid-cols-3 items-center gap-4">
            <Label for="width">Width</Label>
            <Input
              id="width"
              type="text"
              default-value="100%"
              class="col-span-2 h-8"
            />
          </div>
          <div class="grid grid-cols-3 items-center gap-4">
            <Label for="max-width">Max. width</Label>
            <Input
              id="max-width"
              type="text"
              default-value="300px"
              class="col-span-2 h-8"
            />
          </div>
          <div class="grid grid-cols-3 items-center gap-4">
            <Label for="height">Height</Label>
            <Input
              id="height"
              type="text"
              default-value="25px"
              class="col-span-2 h-8"
            />
          </div>
          <div class="grid grid-cols-3 items-center gap-4">
            <Label for="max-height">Max. height</Label>
            <Input
              id="max-height"
              type="text"
              default-value="none"
              class="col-span-2 h-8"
            />
          </div>
        </div>
      </div>
    </PopoverContent>
  </Popover>
</template>

<script setup lang="ts">
import { Button } from '@/Components/Button'
import { Input } from '@/Components/Input'
import { Label } from '@/Components/Label'
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from '@/Components/Popover'
</script>
```

## State and focus

The popover owns its state by default and closes on Escape or an outside interaction. Use `default-open` only for an initial uncontrolled state, and bind `v-model:open` when an external event or completed action must close it. Set `modal` only when outside interaction really must be unavailable while the popover is open.

Keep keyboard focus order logical inside interactive content. A popover should not contain a large form or a decision whose consequence needs a modal confirmation.

## Positioning and portals

`PopoverContent` forwards positioning props such as `side`, `align`, offsets, and collision behavior. StackTrace defaults to centered alignment with a four-pixel side offset. Use `PopoverAnchor` when positioning should follow a different element than the trigger, and use the content `to` prop only when the portal needs a custom destination.

## Composition

Use `PopoverTrigger as-child` to preserve an existing button as the trigger. The child must stay focusable and forward merged attributes and events as described in the [composition fundamentals](https://ui.stacktrace.sk/docs/fundamentals/composition).

> **Use Tooltip for a short explanation: [Tooltip](https://ui.stacktrace.sk/docs/components/tooltip)**
>
> Tooltip is lighter-weight when the content is a brief, non-interactive label or hint. Popover is for content the user deliberately opens and may interact with.

Hosted documentation: [Popover](https://ui.stacktrace.sk/docs/components/popover)
