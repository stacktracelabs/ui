<!-- Generated by scripts/generate-boost-skill-docs.mjs. Do not edit directly. -->

# Menubar

Presents persistent, keyboard-accessible menus for application commands.

## Installation

Add the Menubar component from the configured StackTrace registry.

```shell
npx shadcn-vue@latest add @stacktrace/menubar
```

## Usage

Use a menubar for a command-heavy application whose actions naturally fit stable categories such as File, Edit, View, or Help. Keep the root visible and preserve the menu order across screens. Do not use Menubar for ordinary site navigation; Navigation Menu, Sidebar, or plain links communicate destinations more clearly.

**Menubar example**

```vue
<template>
  <Menubar>
    <MenubarMenu>
      <MenubarTrigger>File</MenubarTrigger>
      <MenubarContent>
        <MenubarGroup>
          <MenubarItem>New tab <MenubarShortcut>⌘T</MenubarShortcut></MenubarItem>
          <MenubarItem>New window <MenubarShortcut>⌘N</MenubarShortcut></MenubarItem>
        </MenubarGroup>
        <MenubarSeparator />
        <MenubarItem>Print <MenubarShortcut>⌘P</MenubarShortcut></MenubarItem>
      </MenubarContent>
    </MenubarMenu>
    <MenubarMenu>
      <MenubarTrigger>Edit</MenubarTrigger>
      <MenubarContent>
        <MenubarItem>Undo <MenubarShortcut>⌘Z</MenubarShortcut></MenubarItem>
        <MenubarItem>Redo <MenubarShortcut>⇧⌘Z</MenubarShortcut></MenubarItem>
      </MenubarContent>
    </MenubarMenu>
  </Menubar>
</template>

<script setup lang="ts">
import {
  Menubar,
  MenubarContent,
  MenubarGroup,
  MenubarItem,
  MenubarMenu,
  MenubarSeparator,
  MenubarShortcut,
  MenubarTrigger,
} from '@/Components/Menubar'
</script>
```

## Menu structure

Give each `MenubarMenu` a concise trigger, group related commands, and separate unrelated groups. Use a submenu only when its parent label describes a genuine command hierarchy. Too many levels make commands difficult to discover and operate with a pointer.

Labels are non-interactive headings. Shortcut components render visual hints only; register the corresponding keyboard actions separately and avoid overriding browser or operating-system shortcuts.

## Choices and state

Use checkbox items for independent toggles and a radio group for one value among several. Bind their models to the same state used by the application so reopening a menu always reflects the current choice.

```vue
<MenubarCheckboxItem v-model="showStatusBar">
  Show status bar
</MenubarCheckboxItem>
<MenubarRadioGroup v-model="zoom">
  <MenubarRadioItem value="100">100%</MenubarRadioItem>
  <MenubarRadioItem value="125">125%</MenubarRadioItem>
</MenubarRadioGroup>
```

The Menubar root can be controlled with `v-model` when a parent must observe or change which menu is open. Prefer uncontrolled state otherwise. Disabled items can explain a temporarily unavailable command, but omit irrelevant commands rather than filling the menu with disabled rows.

## Destructive actions

Use `variant="destructive"` for commands such as deleting a document, not for ordinary close or cancel actions. If activation would cause a significant irreversible result, open a confirmation dialog after selection and allow focus to move into that dialog normally.

> **Confirm significant destructive commands: [Confirmation Dialog](https://ui.stacktrace.sk/docs/components/confirmation-dialog)**
>
> Confirmation is appropriate when undo is unavailable and the cost of an accidental command is high.

## Accessibility and composition

ArrowLeft and ArrowRight move between top-level triggers. ArrowDown opens a menu, arrow keys move within it, Enter or Space selects, and Escape closes it. Focus remains within the menubar interaction until the user selects a command or moves away.

Use `as-child` only when the composed element preserves menu roles, focus, and event forwarding. See the [composition fundamentals](https://ui.stacktrace.sk/docs/fundamentals/composition). Use Dropdown Menu when only one trigger is needed.

Hosted documentation: [Menubar](https://ui.stacktrace.sk/docs/components/menubar)
